1.CoreSpawnner is the Gameobject that contains all the requred scrips 
2.Populate SquareDataSet 
 a.Save Name - Name the data set
 b.Set the BaseGrid size x and y
 c.Populate SquareData (note x and y denotes the cordinates on the grid)
3.Data Handler has a ContextMenu for save and load

After saving the data hit play.

If you want to load a different previously saved data set type in the save name in the LoadFile text fild and hit the Load contextMenu

	